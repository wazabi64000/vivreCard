info:
  name: varify (1)
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}/users/verify/"
  body:
    type: json
    data: |-
      {
        "email": "tobak66551@cslua.com",
        "password" :"Azerty123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
